server {
    listen      80;
    listen [::]:80;
    server_name 3.222.215.255;

    location / {
        rewrite ^ https://$host$request_uri? permanent;
    }

    location ^~ /.well-known {
        allow all;
        root  /data/letsencrypt/;
    }
}
